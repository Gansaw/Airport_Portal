<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
<meta charset="UTF-8">
    <meta charset="UTF-8">
    <title>공지사항</title>
</head>
<body>
    <h2>공지사항</h2>
    <table>
        <thead>
            <tr>
                <th scope="col">번호</th>
                <th scope="col">제목</th>
                <th scope="col">글쓴이</th>
                <th scope="col">날짜</th>
                <th scope="col">조회수</th>
            </tr>
        </thead>
        <tbody>            
			<tr th:each="board, state : ${noticeList}">
				<td th:text="${state.count}">
				<td><a th:href="@{/getNotice(seq=${notice.id})}" th:text="${notice.title}"></a></td>				
				<td th:text="${notice.writer}">
				<td th:text="${#dates.format(notice.date,'yyyy-MM-dd')}"></td>
				<td th:text="${notice.view}">			
			</tr>
        </tbody>
    </table>
</body>
</html>
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
<meta charset=UTF-8">
    <title>갤러리</title>
</head>
<body>
    <h2>갤러리</h2>
    <table>
        <thead>
            <tr>
                <th scope="col">번호</th>
                <th scope="col">제목</th>
                <th scope="col">글쓴이</th>
                <th scope="col">날짜</th>
                <th scope="col">조회수</th>
            </tr>
        </thead>
        <tbody>            
            <tr th:each="notice : ${gallery}">
                <td th:text="${gallery.id}"></td>
                <td th:text="${gallery.title}"></td>
                <td th:text="${gallery.writer}"></td>
                <td th:text="${gallery.date}"></td>
                <td th:text="${gallery.view}"></td>
            </tr>
        </tbody>
    </table>
</body>
</html>
<!DOCTYPE html>
<html lang="ko">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>로그인</title>
</head>

<body>
    <h1>로그인</h1>
    <form action="/login" method="post" th:object="${loginForm}">
        <div class="grid">
            <label for="username">
                아이디
                <input type="text" id="username" name="username" th:field="*{username}">
            </label>
            <label for="password">
                비밀번호
                <input type="password" id="password" name="password" th:field="*{password}">
            </label>
        </div>
        <button type="submit">로그인</button>
        <button type="button" onclick="location.href='/logins/signup'">회원가입</button>
        <!-- 아이디가 입력되지 않은 경우 에러 메시지를 표시합니다. -->
        <p th:if="${loginError}" th:text="${loginError}" style="color: red;"></p>
    </form>
</body>

</html>